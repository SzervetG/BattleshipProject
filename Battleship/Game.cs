namespace Battleship;

public class Game
{
    /* Ship placing rule:
     
       unit = Convert.ToInt32(Math.Floor(boardsize * boardsize / 2 % 15));
       5 Long * unit
       4 Long * unit + 1
       3 Long * unit + 2
       2 Long * unit + 3
       1 Long - Convert.ToInt32(Math.Floor(boardsize * boardsize / 2)) - (4*unit + 6)
    */


    public void Gameplay()
    {
        
    }
}